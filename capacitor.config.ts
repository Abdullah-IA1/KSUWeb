import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ksu',
  appName: "'ksu'",
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
